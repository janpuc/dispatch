image:
  repository: ghcr.io/janpuc/dispatch-operator
  tag: main
  pullPolicy: Always

gateway:
  port: 9080

metricsPort: 8080
healthPort: 8081

otlpEndpoint: ""
sessionsPVC: ""

resources:
  requests:
    cpu: 10m
    memory: 64Mi
  limits:
    memory: 256Mi

serviceMonitor:
  enabled: false

agentServiceAccounts:
  enabled: true
